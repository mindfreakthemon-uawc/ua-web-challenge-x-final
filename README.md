# ua-web-challenge-x-final

### To run this piece:
* Run http server in project directory and open index.html

### Making changes
* `npm install` project
* run `gulp` to build dev version / run `gulp dev:prod` to build prod version
